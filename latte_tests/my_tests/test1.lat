/* test arithmetic and comparisons. */

int main() {
    int x = 56;
    int y = -23;
    printInt(x+y);
    printInt(x-y);
    printInt(x*y);
    printInt(45/2);
    printInt(78%3);
    printbool(x-y > x+y);
    printbool(x/y <= x*y);
    printString("string"+" "+"concatenation");
    return 0 ;
}

void printbool(boolean b) {
  if (b) {
    printString("true");
    return;
  } else {
    printString("false");
    return;
 }
}
